<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SelectedApp" >

    <TextView
        android:id="@+id/TVAppMenu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_menu_title" />  
    
    <Button
        android:id="@+id/btnSessions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/TVAppMenu"
        android:layout_below="@+id/TVAppMenu"
        android:text="@string/btnSessionsText" />
    
	<Button
	    android:id="@+id/btnStates"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignLeft="@+id/TVAppMenu"
	    android:layout_below="@+id/btnSessions"
	    android:text="@string/btnStatesText" />
	
	<Button
        android:id="@+id/btnViewSettings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/TVAppMenu"
        android:layout_below="@+id/btnStates"
        android:text="@string/btnViewSettingsText" />
    
</RelativeLayout>
